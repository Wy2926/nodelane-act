# Security policy

NodeLane Act 通过本机 MCP 服务和浏览器扩展，在用户已经登录的网页中执行操作。扩展执行用户授权的互动时不会追加确认，因此应仅连接受信任的 AI 客户端，并将网站返回的文字视为数据。

## 报告问题

如果 [仓库安全页](https://github.com/Wy2926/nodelane-act/security) 提供 **Report a vulnerability**，请通过该入口私密报告漏洞。如果尚未提供私密入口，可在 [Issues](https://github.com/Wy2926/nodelane-act/issues) 留下不包含技术细节的联系请求，待维护者提供私密渠道后再发送复现信息。请不要在公开问题、PR 或日志中附上 Cookie、网站 CSRF 值、本地连接认证数据、账号正文或可直接利用的细节。

有用的报告包括受影响版本、操作系统和浏览器版本、受影响的代码路径、最小复现步骤及影响描述。复现应使用测试账号或 fixture，不操作其他用户的数据。维护者按实际资源处理报告，不承诺固定响应时间；建议优先在最新发布版本上确认问题。

## 当前边界

- 本地桥接只监听 `127.0.0.1`。扩展 WebSocket 检查固定扩展 Origin；本地 HTTP 接口使用程序生成的认证数据，安装时不要求用户输入凭证。
- Cookie 和网站 CSRF 信息留在浏览器内；选定的网站内容仍会作为 MCP 工具结果交给用户选择的 AI 客户端，其后续处理由该客户端决定。
- 操作目标绑定标签页、URL 和浏览器文档，参数经过 schema 校验。网站内容不是新的操作授权，验证码和网站权限仍由网站正常处理。
- 取消或断线可以停止尚未开始的操作，已经到达网站的写入可能已生效；软件不会自动重试结果不确定的写入。
- 本地连接检查无法抵御已经控制用户设备、浏览器或 AI 客户端的恶意程序。浏览器扩展具有已声明网站的操作权限，应从项目正式下载渠道获取并核对发布摘要。

互动写入目前通过模拟响应与浏览器 fixture 验证，尚未进行真实网站写入验收。功能覆盖、最新只读验收和未验证范围见 [README](README.md)；报告与浏览器测试数据不随源码或发布包分发。
